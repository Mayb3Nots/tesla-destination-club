export interface Vehicle {
    id: string;
    userId: string;
    plateNumber: string;
    model: string;
    trim: string;
    color: string;
    year: number;
    createdAt: string;
    updatedAt: string;
}
