export interface Vehicle {
    id: string;
    userId: string;
    plateNumber: string;
    make: string;
    model: string;
    color: string;
    year: number;
    createdAt: string;
    updatedAt: string;
}
